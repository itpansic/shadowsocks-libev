{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "ss-server build DEBUG",
            "type": "shell",
            "command": "mkdir -p build; cd ./build; cmake -DCMAKE_BUILD_TYPE=Debug ..; make ss-server;",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}